{
  "name": "@lanterna-profiler/detectors",
  "version": "2.0.0",
  "description": "Lanterna default detector pack — CPU, memory & async detectors, thresholds, attribution helpers, plugin types",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist/**/*.d.ts",
    "dist/**/*.js",
    "dist/**/*.map",
    "LICENSE"
  ],
  "scripts": {
    "build": "tsc -b",
    "dev": "tsc -b --watch",
    "test": "vitest run",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "clean": "rm -rf dist .turbo .tsbuildinfo"
  },
  "dependencies": {
    "@lanterna-profiler/core": "^2.0.0"
  },
  "devDependencies": {
    "zod": "^4.4.1"
  },
  "engines": {
    "node": ">=22.0.0"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/arkerone/lanterna.git",
    "directory": "packages/detectors"
  },
  "license": "MIT"
}
