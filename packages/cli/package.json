{
  "name": "@lanterna-profiler/cli",
  "version": "1.10.0",
  "description": "Agent-first Node.js CPU, memory & async profiler — CLI",
  "type": "module",
  "bin": {
    "lanterna": "./bin/lanterna.js"
  },
  "files": [
    "bin",
    "dist/**/*.d.ts",
    "dist/**/*.js",
    "dist/**/*.map",
    "LICENSE"
  ],
  "scripts": {
    "pretest": "npm run build",
    "build": "tsc -b",
    "dev": "tsc -b --watch",
    "test": "vitest run",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "clean": "rm -rf dist .turbo .tsbuildinfo"
  },
  "dependencies": {
    "@clack/prompts": "^1.3.0",
    "@lanterna-profiler/core": "^2.0.0",
    "@lanterna-profiler/detectors": "^2.0.1",
    "chalk": "^5.6.2",
    "cli-table3": "^0.6.5",
    "commander": "^14.0.3",
    "gradient-string": "^3.0.0",
    "ora": "^9.4.0",
    "ps-list": "^9.0.0",
    "zod": "^4.4.3"
  },
  "engines": {
    "node": ">=22.0.0"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/arkerone/lanterna.git",
    "directory": "packages/cli"
  },
  "license": "MIT"
}
