---
"@lanterna-profiler/cli": minor
---

Refresh the CLI UI/UX: replace the large lantern illustration with a compact ANSI Shadow wordmark in a warm amber-to-gold gradient, add a single-line breadcrumb header for subcommand help, regroup options into clear sections (Capture, Memory kind, Output, Plugins, General), add `--version` / `-v`, surface a concise error on unknown commands, append relative timing hints to persisted activity steps, expose an `info()` step on the indicator, and tighten the attach picker with a compact header and navigation hint.
